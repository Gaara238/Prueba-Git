let board = [
  [null, null, null, null],
  [null, null, null, null],
  [null, null, null, null],
  [null, null, null, null],
];

class Pair {
  constructor(img, position1, position2) {
    this.img = img;
    this.position1 = position1;
    this.position2 = position2;
  }
}

function createPair() {
    let img = 
}

function chooseImg() {
    let randomNumber = Math.floor(Math.random() * 7);
    if (randomNumber === 0) {
      return ""
    }
}
